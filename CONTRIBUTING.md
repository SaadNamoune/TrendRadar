# Contributing

```bash
git clone https://github.com/SaadNamoune/TrendRadar.git
cd TrendRadar && pip install -r requirements.txt
pytest tests/ -q
```

PRs welcome. Keep commits in `type: description` format. Add tests for new scoring rules or category keywords.
